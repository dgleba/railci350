
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_lkup_sugg_status_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing Lkup Sugg Statuses</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :name_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @lkup_sugg_statuses %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @lkup_sugg_statuses, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Name</th>
      <th>Sort</th>
      <th>Active status</th>
    </tr>
  </thead>

  <tbody>
    <% @lkup_sugg_statuses.each do |lkup_sugg_status| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', lkup_sugg_status, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_lkup_sugg_status_path(lkup_sugg_status), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Destroy', lkup_sugg_status, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= lkup_sugg_status.name %></td>
        <td><%= lkup_sugg_status.sort %></td>
        <td><%= lkup_sugg_status.active_status %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
